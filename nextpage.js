document.getElementById('generateBtn').addEventListener('click', function() {
    // check if more than 5 characters are selected on the active webpage
    // use chrome.tabs.executeScript or chrome.tabs.sendMessage to communicate with content script
});

document.getElementById('notesBtn').addEventListener('click', function() {
    // show the notes page
});
