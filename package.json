{
  "name": "lecture-note-chrome-extension",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src tests eslint.config.js",
    "test": "node --test",
    "test:coverage": "node --experimental-test-coverage --test",
    "validate": "npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.11.0",
    "globals": "17.12.0"
  },
  "engines": {
    "node": ">=24"
  }
}
